面向对象的三大特征：
封装：
继承：
多态：
extends或者是implements是多态的前提
小明是一个学生也是一个人类，小明是一个对象，这个对象既有学生形态也有人类形态，
一个对象拥有多种形态就叫做对象的多态性
对象的向上转型：其实就是多态 父类对象 对象名 = new 子类对象();左父有子
含义：创建一个子类对象，把他当作父类看待使用 Animal animal = new Cat();
                                    创建了一只猫，把他当作动物看待，没问题
注意事项：向上转型一定是安全的，类似于：自动类型转换从小范围型转向大范围，从小范围的猫转换为更大范围的动物
