哈希表的特点：查询速度快
HashSet集合存储数据的结构：（哈希表）
在JDK1.8之前 哈希表 = 数组 + 链表 ;
在JDK1.8之后 哈希表 = 数组 + 链表 ;
            哈希表 = 数组 + 红黑树 (提高查询效率);
哈希表的特点：查询速度快
数组初始容量为16 0~15把相同哈希值的元素分为一组，链表（红黑树）结构把相同哈希值的元素链接到一起
存储元素到hashset中：1.先计算元素的哈希值
两个元素不同但是哈希值相同叫做哈希冲突，重地和通话
如果链表的长度超过8位就会把链表转换为红黑树